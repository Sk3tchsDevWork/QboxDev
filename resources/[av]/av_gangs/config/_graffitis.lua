-- Key index is the gang name and the value is the object name from assets_grafitis/stream/ without the .ydr extension (read the docs)
-- Key index is the gang name and the value is the object name from assets_grafitis/stream/ without the .ydr extension (read the docs)
-- Key index is the gang name and the value is the object name from assets_grafitis/stream/ without the .ydr extension (read the docs)

allGangs = {
    ['ballas'] = "spray_ballas",
}